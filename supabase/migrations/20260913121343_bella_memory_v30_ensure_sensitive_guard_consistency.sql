comment on function public.bella_memory_save_v30(text,text,text,text,smallint,smallint,smallint) is
  'Bella v30 explicit durable memory writer. SECURITY INVOKER + RLS, server-side sensitive-memory guard, topic-aware contradiction supersession, bounded topic keys.';
